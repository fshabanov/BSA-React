export enum HttpMethods {
	GET = 'GET',
	POST = 'POST',
	DELETE = 'DELETE',
}

export enum ApiPath {
	AUTH = '/auth',
	CURRENT_USER = '/authenticated-user',
	SIGN_IN = '/sign-in',
	SIGN_UP = '/sign-up',
}
